export enum EDashboardTabs {
  Overview = "overview",
  Expenses = "expenses",
  Savings = "savings",
}

export enum EAuthTabs {
  Register = "register",
  Login = "login",
}

export enum ETheme {
  Light = "light",
  Dark = "dark",
  System = "system",
}
